package com.nthg.matrixcamera

import android.app.Application

/**
 * Application class for Matrix Camera.
 * Initializes app-level components.
 */
class MatrixCameraApplication : Application() {

    override fun onCreate() {
        super.onCreate()
        // Future: Initialize crash reporting, analytics opt-out, etc.
    }
}
